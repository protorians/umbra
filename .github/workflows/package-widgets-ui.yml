name: widgets-ui Package Workflow

on:
  push:
    branches: [ dev, main, stable, publish-next ]
    paths:
      - 'packages/widgets-ui/**'
      - '.github/workflows/package-widgets-ui.yml'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
  workflow_dispatch:

jobs:
  publish-dev:
    if: github.ref == 'refs/heads/dev'
    uses: ./.github/workflows/reusable-publish.yml
    with:
      package: widgets-ui
      npm_tag: dev
      build_env: prod
    secrets: inherit

  publish-release:
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/reusable-publish.yml
    with:
      package: widgets-ui
      npm_tag: latest
      build_env: prod
    secrets: inherit

  publish-stable:
    if: github.ref == 'refs/heads/stable'
    uses: ./.github/workflows/reusable-publish.yml
    with:
      package: widgets-ui
      npm_tag: stable
      build_env: prod
    secrets: inherit

  publish-next:
    if: github.ref == 'refs/heads/next'
    uses: ./.github/workflows/reusable-publish.yml
    with:
      package: widgets-ui
      npm_tag: next
      build_env: prod
    secrets: inherit
